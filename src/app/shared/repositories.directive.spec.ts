import { RepositoriesDirective } from './repositories.directive';

describe('RepositoriesDirective', () => {
  it('should create an instance', () => {
    const directive = new RepositoriesDirective();
    expect(directive).toBeTruthy();
  });
});
