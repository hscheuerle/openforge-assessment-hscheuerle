import { ColorBreakDirective } from './color-break.directive';

describe('ColorBreakDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorBreakDirective();
    expect(directive).toBeTruthy();
  });
});
